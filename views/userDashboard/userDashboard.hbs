{{> loginNavbar}}

<div class="container bootstrap snippets bootdeys mt-5 mb-5">
    <div class="row">
        <div class="col-xs-12 col-sm-12">
            <div class="panel panel-default">
                <div class="panel-body text-center">
                    <img src="{{user.picture}}" class="img-circle profile-avatar" alt="User avatar">
                    <p class="panel-title">Welcome Mr. <strong>{{user.displayName}}</strong></p>
                    <h4>{{user.email}}</h4>
                </div>
            </div>
        </div>
    </div>
</div>
</div>